server:
  port: 9092
  
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
       dialect = org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/prudential-onboarding
    username: postgres
    password: admin
    driverClassName: org.postgresql.Driver
  application:
    name: USER-SERVICE
  config:
        activate:
            on-profile: dev
#  zipkin:
#    base-url: http://127.0.0.1:9411/
    
logging:
  file.name: C:/Users/002TCO744/Desktop/elk-logs/user-service.log

#department:
#  service:
#    url: http://DEPARTMENT-SERVICE

#eureka:
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url: 
#      defaultZone: http://localhost:8761/eureka/
#  instance:
#    hostname: localhost
   
  